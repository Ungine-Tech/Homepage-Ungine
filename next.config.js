// @ts-check

/** @type {import("next").NextConfig}  */
const nextConfig = {
    swcMinify: true,
    reactStrictMode: true,
    compress: true,
    output: "standalone",
};

module.exports = nextConfig;
